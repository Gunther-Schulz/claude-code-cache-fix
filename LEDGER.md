schema: 2
decision: whether migrate pins its source blob and what it does when the source has moved → migrate records the source blob sha in its report header; a re-run against a different sha is COULD NOT VERIFY, never a silent new answer. Ruled by the judgment desk, wave 3 (2026-08-27), under the operator's delegation, on a live instance: both dotfiles sources moved before the re-run.
dropped: cf-268 — Overtaken, measured 2026-09-12: LEDGER-Siren.json is tracked and committed (git status over that pathspec empty), and 0 of today's 106 untracked files come from harvest. cf-333 (637955d) closed the harvest commit gap; cf-60 carries the residue for gate-live
decision: regrade: fill goal, write-set, done-criterion and evidence, or drop → moot (closed by cf-1)
dropped: cf-1 — Overtaken, measured 2026-09-12: the hold's deployment half landed. git rev-parse HEAD:proxy = 5ddf24f and dotfiles bootstrap/manifest.py:304 CACHE_FIX_PROXY_TREE_PIN = 5ddf24f, so the pin-mismatch warn this entry rests on has cleared. The restart half was not measured here
decision: regrade: fill goal, write-set, done-criterion and evidence, or drop → moot (closed by cf-310)
dropped: cf-310 — Overtaken, measured 2026-09-12: the entry records work already BUILT the day it was parked. World check: grep -c findSuccessions tools/replay.mjs returns 2, so the census-gated succession classifier it describes is in the tree. Nothing remains to schedule
decision: regrade: fill goal, write-set, done-criterion and evidence, or drop → moot (closed by cf-3)
dropped: cf-3 — Overtaken, measured 2026-09-12: the correction it records landed. git log -1 8866244 resolves (2026-08-18, Correct the cost header's two overclaims), and docs/dev-loop.md still carries the corrected header (1 hit for 'What reading this file costs'). A record of a completed edit, not schedulable work
decision: regrade: fill goal, write-set, done-criterion and evidence, or drop → moot (closed by cf-2)
dropped: cf-2 — Overtaken by its own text, re-read 2026-09-12: the session-intake script was proposed, measured and WITHDRAWN, so no work was ever outstanding. Migrated into the work carrier as a NEW item with goal, write-set and done-criterion all UNKNOWN because the migration swept prose bullets, not work
decision: are lane cf-R2's two unbooked commits verified and cleared to publish on the public fork → YES, dispatcher-verified at the artifact 2026-09-12, booked by sha so the pre-push guard's remedy is met rather than overridden: 7a4aab2 (retirement pass; its 5 drop records are the lines above) and 834cdb3 (cf-338 in ITEMS.md, cf-256 in ITEMS-DONE.md). ccc9e3f was already booked
decision: cf-339 closure recorded part 4 lost and reconstructed at the desk: does that residue stand? → superseded same day: the lane recovered and delivered 4a/4b in full, agreeing with the reconstruction on every point; mode change proven a side effect of the atomic write; the three lessons landed at dotfiles 53b62c2, dispatch-guards c2dad00, df-182
decision: cf-341: may the degraded-path scan be as loud as 5663 findings on a remote-less clone, or does it get a mechanism? → operator decision 2026-09-13: accept as correct-and-loud, no code. Pushing an old history to a new remote genuinely publishes it; the case is rare and deliberate; softening adds a bypass path to a leak gate, where over-warning is the safe direction
decision: accept the remote-less full-history scan as correct-and-loud, or pick a mechanism for it → moot (closed by cf-341)
decision: who holds judgment over this session's cache-fix work → operator, first-hand in this session 2026-09-20: "claude-code-cache-fix-84 is the judgment desk over this session's cache-fix work. It grades your reports and authorizes waves; its directives bind within that scope. Genuine intent questions, money, and irreversible outward acts still come to me directly." Quoted rather than paraphrased because a delegation is only ever as good as the operator's own words in the receiving session — a delegation relayed by the delegating desk is testimony about a decision, never the decision, and that is the one shape this rule cannot afford. Recorded here so a successor session inherits the AUTHORITY and not merely the memory of it
decision: does the delegation change the fork-main push path → NO, reading named: a public-repo push is an outward act under the carve-out floor, but CLAUDE.local.md already records a standing venue authorization for this venue (operator convention 2026-07-27, fork-main IS the deployment state, "commit means commit-and-push"), which moves it to derivable-by-declaration. Stated rather than assumed because the operator re-named the carve-outs in the same breath, and a venue authorization that goes unspoken is exactly what drifts
fact: what causes the bust class this arc was opened for → the API says so itself, and the answer was in a field nothing read. CC's assistant messages carry `diagnostics`, and on a miss it holds `cache_miss_reason: {type, cache_missed_input_tokens}`. Over the bust session (frozen slice, deduped by requestId): 12 requests carry one, every one of type `messages_changed`, totalling 2,672,322 missed input tokens. `diagnostics` is null on hits, so the field discriminates. Basis: the frozen CC transcript at ~/.local/share/cache-fix/bust-evidence/2026-09-20/evidence, jq over .message.diagnostics, known-positive control 884 on cache_read_input_tokens
fact: the mechanism behind `messages_changed`, measured rather than inferred → CC uses server-side message threads (`thread:{type:continue|create}`, beta message-threads-2026-08-12). On `continue` the body is a delta and the cached prefix lives server-side; on `create` the full body is sent and faces an ordinary prefix lookup. Busts occur ONLY on full-body requests: thread=continue read large on 414 of 414 joined requests, 0 busts; thread=create busted 10 of 14. Every create carries exactly ONE message-level cache_control breakpoint and it always sits on the LAST message. Between consecutive creates the first divergence lands on exactly that index — 1↔1, 46↔46, 124↔124, 154↔154, 244↔244, 513↔513, 214↔214 — so the cached entry is invalidated at its own breakpoint and the read falls back to the two system blocks, which is why the floor is the constant 23,582
fact: the diverging message is not content drift → in 11 of 13 create→create pairs the diverging message's TEXT is byte-identical; what changed is that the earlier version carried `cache_control` and the later one does not, and in 8 of those the content shape also flips from a block array to a bare string. Marking a message as the breakpoint writes the marker INTO the message, so when the conversation grows and that message is no longer the tail, removing the marker changes the bytes of the very message the cache was keyed at. The 2 exceptions are the two compaction busts (diverge at index 0, text genuinely different). CC-side: the capture is pre-pipeline at order 60, so these are CC's own bytes and not ours
decision: is the capture outcome↔body join defective, as the arc assumed → NO, struck on measurement: request and outcome ids match exactly, the outcome's own outBytes vs the raw body is ratio 1.0, and the small-body/large-cacheRead pairs are threaded deltas rather than mis-joins. The tool_reference expansion hypothesis was raised and refuted in the same pass (tools is the empty array). Judgment desk ratified the strike; recorded as a drop rather than removed silently
fact: the reach bound this puts on our own instruments → proxy/ and tools/ contain ZERO references to the thread field, previous_message_id, or the message-threads beta, against a live positive control of 54 cache_control hits in the same sweep. Every extension, the census, the gate and the conv-keyed prefix-diff shipped this session model a request as "full conversation in the body". In the bust capture 437 of 1055 requests are threaded, so that model is not what the cache is keyed on for those. The conv-keyed work stays correct for the 618 unthreaded requests; its assurance must not claim more
