{
  "_": "STATUS IS DATA; the matrix keeps the reasoning. One object per threat-matrix row, keyed by row number. Populated 2026-08-11 by reading each row's BODY to its deciding sentence, never its leading token — the misread that produced this file. `date` is the date of the DECIDING EVIDENCE, not of this write, so a stale verdict is visible as an old date. Enum and rules: docs/directives/records-restructure.md, phase 1. Checker: test/matrix-status.test.mjs. The build board is `jq '[to_entries[]|select(.value.status==\"OPEN\" or .value.status==\"RESIDUAL\")]|from_entries' docs/directives/robustness-threat-matrix.status.json`.",
  "1": {
    "status": "RESIDUAL",
    "evidence": "proxy/extensions/insertion-normalization.mjs",
    "date": "2026-07-28",
    "residual": "NEAR-ZERO, not zero. The cell's leading clause still names the bookmark ladder as the mitigated half; that mechanism was RETIRED 2026-07-28 (matrix section 'Bookmark-ladder disposition — REVERSED', measured: the ladder itself produced 57 stability violations). What carries the class today is insertion-normalization alone, and the insertions that still bust are rows 4 and 22's classes."
  },
  "2": {
    "status": "ACCEPTED",
    "evidence": "docs/directives/robustness-threat-matrix.md",
    "date": "2026-08-14",
    "residual": "WON'T BUILD (operator decision 2026-08-14): the phase-3 keepalive is not built, so an idle-hour cold is expected cost and a 1h-idle bust triages EXPECTED-BUST. A keepalive is cost-positive only if the operator RETURNS inside the extended window, and the deliberate case is already covered by their own keep-warm skill, which fires exactly when it pays off; a proxy-side timer has to guess, and a wrong guess bills a full-prefix read per idle window on every session the machine holds open. The 2026-07-30 measurable build trigger (a week of TTL-idle colds with return inside ~2h) is WITHDRAWN as the decision criterion. Not-built design of record, kept should the decision reverse: docs/directives/proxy-ttl-keepalive.md. Residual risk accepted with it: an idle-then-return the skill was not started for pays the full cold, and nothing measures how often that happens — the rate was never instrumented and is not being instrumented."
  },
  "3": {
    "status": "RESIDUAL",
    "evidence": "docs/audits/restart-state-audit.md",
    "date": "2026-07-31",
    "residual": "Restart transparency is STRONGLY SUPPORTED, NOT PROVEN by the cell's own words: one live observation, with code changing alongside it that happened to be output-neutral on that traffic. The standing exception is unchanged and is a live obligation, not a caveat — a change touching state KEYS or freeze logic is NOT cache-transparent and owes its row-3 declaration BEFORE the restart (exercised 2026-08-10 by D1, row 26)."
  },
  "4": {
    "status": "OPEN",
    "evidence": "059aae3",
    "date": "2026-08-13",
    "residual": "BLOCKED at the byte-match census as of 2026-08-13, which is a harder state than the previous 'canonicalization build, booked READY'. Full-corpus run (41/41 captures, 0 UNREADABLE, 15737 same-conversation pairs): 483 EXACT / 54 EXTENDED (all MERGED-STANDALONE, 0 NEW-TEXT) / 12 DROPPED / 16 MISMATCH. The tool's own verdict is 'DO NOT SHIP as-is' -- any MISMATCH means the canonical form differs from CC's and normalizing would MOVE the bust rather than absorb it. The placement half fails independently: over the 483 EXACT rows the standalone-minus-host offset takes 20 distinct values (+1 on 457, then 19 others out to +110), and the tool prints 'MORE THAN ONE PLACEMENT -- a mitigation cannot pick an index that is right every time'; correct bytes at the wrong index diverge the prefix just the same. Both prior bounding facts still stand and are now joined by a third: the class has two shapes and a rule tuned on one has no defined behaviour on the other; the RATE is unmeasured; and the canonical rule as designed does not hold on 16 real occurrences. Unread residue: only 1 of the 16 MISMATCH rows appears in default output (2026-08-11T16:36:32.829Z host=3 recon=5376ch rejected=10014ch) -- the other 15 need --verbose and are what decides whether the 16 are one shape or several."
  },
  "5": {
    "status": "DECLINED",
    "evidence": "docs/directives/robustness-threat-matrix.md",
    "date": "2026-07-27",
    "residual": "Building would be wrong, and the cell says so in one line: a /reload-plugins mid-session is a TRUE content change, so suppressing the delta would suppress a legitimate bust. Known volatile fields are already covered by fingerprint-strip, identity-normalization and cc-version-normalize; the remainder is honest."
  },
  "6": {
    "status": "RESIDUAL",
    "evidence": "6fc397d",
    "date": "2026-08-10",
    "residual": "The mitigation ships (deferred-tool-rewrite holds tools[] byte-stable; heldStable rides every sweep since 813edc8) and the tools-ONLY isolation this row asked for since 2026-07-27 finally arrived 2026-08-10. Three things stay open: the original 2026-07-27 12:47:56 event still carries TWO independent causes and no tools-only delta was ever isolated for it; the 2026-08-10 isolation rests on ONE pin; and `passthrough` has never been observed non-empty on real traffic, so its two reason branches are proven reachable by constructed bites only."
  },
  "7": {
    "status": "SHIPPED",
    "evidence": "proxy/extensions/sort-stabilization.mjs",
    "date": "2026-07-28",
    "residual": null
  },
  "8": {
    "status": "SHIPPED",
    "evidence": "fc432bf",
    "date": "2026-07-27",
    "residual": null
  },
  "9": {
    "status": "RESIDUAL",
    "evidence": "proxy/extensions/image-strip.mjs",
    "date": "2026-07-27",
    "residual": "CLOSED-PRESUMED, and the presumption is the remainder: no measured instance and no regression fixture. The cell's own condition stands — add one fixture if an image-bearing session ever shows a mid-history image diff."
  },
  "10": {
    "status": "ACCEPTED",
    "evidence": "docs/dev-loop.md",
    "date": "2026-07-28",
    "residual": "Honest bust, operator-initiated (/rc from session start; compaction unavoidable at the window limit). Settled by replay: compaction is a NEW conversation, the prefix changes at index 0, so no cached bytes survive by construction. Standing obligation: normalization rule 3 must keep passing these through, which is regression-tested."
  },
  "11": {
    "status": "IMPOSSIBLE",
    "evidence": "docs/directives/robustness-threat-matrix.md",
    "date": "2026-07-27",
    "residual": "The cache is model-keyed, so a model switch re-bills by construction — nothing on our side can hold a prefix across it. The cell's own wording is ACCEPTED (rare, deliberate), which is the same disposition read from the operator's side. Attribution exists: worktime cause=model."
  },
  "12": {
    "status": "UNASSESSED",
    "evidence": "proxy/extensions/thinking-block-sanitize.mjs",
    "date": "2026-07-27",
    "residual": "No measured instance, so there is nothing to assess mitigability against; thinking-block-sanitize covers the known shapes. The alarm signature is named and is the trigger to re-open: prefix-diff cause=messages@N(assistant) with no insertion."
  },
  "13": {
    "status": "SHIPPED",
    "evidence": "proxy/extensions/deferred-tool-rewrite.mjs",
    "date": "2026-07-28",
    "residual": null
  },
  "14": {
    "status": "SHIPPED",
    "evidence": "5a7093d",
    "date": "2026-07-28",
    "residual": null
  },
  "15": {
    "status": "RESIDUAL",
    "evidence": "proxy/extensions/ttl-management.mjs",
    "date": "2026-07-27",
    "residual": "Shipped before the row was written (TTL_SUBAGENT via detectRequestType), then pinned to 5m by operator decision via unit env. Two remainders the cell names: subagent-scoped telemetry (small), and an UNBRIDGED VERIFICATION GAP — no captured-traffic confirmation that subagent requests arrive without an explicit ttl, because the snapshot format drops ttl fields."
  },
  "16": {
    "status": "OUT-OF-SCOPE",
    "evidence": "docs/directives/robustness-threat-matrix.md",
    "date": "2026-07-27",
    "residual": "The mitigation lives outside this repo: an operator hook already catches the fallback reroute and stops the session. This repo's half is attribution only (worktime cause=model)."
  },
  "17": {
    "status": "OUT-OF-SCOPE",
    "evidence": "docs/directives/robustness-threat-matrix.md",
    "date": "2026-07-27",
    "residual": "N/A for this operator's profile (fable sessions); the row exists as a note so the class is not re-derived."
  },
  "18": {
    "status": "SHIPPED",
    "evidence": "323af49",
    "date": "2026-07-28",
    "residual": null
  },
  "19": {
    "status": "SHIPPED",
    "evidence": "proxy/extensions/deferred-tool-rewrite.mjs",
    "date": "2026-07-28",
    "residual": "Covered as serialization by row 13's hold — the definition stays in the array and permission enforcement is harness-side at call time, so holding is semantically safe."
  },
  "20": {
    "status": "ACCEPTED",
    "evidence": "docs/directives/robustness-threat-matrix.md",
    "date": "2026-07-27",
    "residual": "Documented rather than mitigated: the system prompt embeds cwd/platform/git state, so resuming a session on another machine is a guaranteed full re-read. The operating rule is start fresh on the new machine, never resume expecting a warm cache."
  },
  "21": {
    "status": "IMPOSSIBLE",
    "evidence": "docs/directives/robustness-threat-matrix.md",
    "date": "2026-07-27",
    "residual": "Upstream capacity eviction: no local attribution is possible and no local mitigation exists. The standing trap is recorded in the cell and binds any reader — `other`/`unavailable` is a DEGRADED DEFAULT, never a tested-and-rejected verdict, so an undiagnosed bust belongs in an unattributed bucket and must not be closed onto this row."
  },
  "22": {
    "status": "ACCEPTED",
    "evidence": "docs/directives/success-path-only-enumeration.md",
    "date": "2026-08-11",
    "residual": "Settled 2026-08-11. The promoted question that kept this row open — which normalization behaviours silently switch off on a reset — is answered by the cited artifact, and its top finding (validateToolAdjacency runs on no reset path) is booked separately. The original prune-bust mechanism is a MEASURED NON-EVENT (classifyPrune, 404d5fc: 10 of 12 pure-tail prunes produced zero miss events). What remains parked is mitigability of the suggestion-mode turns themselves, blocked on pruning-boundary stability — those turns carry ordinary user/assistant roles, so holding them out on a text match risks dropping real conversation, and safety outranks cache. Re-open on either a cost attributable to the injection/prune cycle ALONE, or a boundary predicate better than a text prefix."
  },
  "23": {
    "status": "RESIDUAL",
    "evidence": "fd87e12",
    "date": "2026-08-13",
    "residual": "SHIPPED fd87e12 (description-absorbed: canonical block forwarded on a description-only delta, new prose announced in-band, name/schema/set/order still take the honest reset) and DEMONSTRATED ABSORBING live 2026-08-13: 114 description-absorbed firings across 2 sessions produced ZERO uncontrolled re-bills. 83 of 114 have no cold event within +/-180s. The 31 that do all cluster around ONE event in one session (cc=308201, written twice 3s apart as cause=other and cause=resume), and the other session's only re-bill in its entire ledger history is cause=compact -- both CONTROLLED causes that rebuild the prefix by construction, which no mitigation can absorb. REMAINDER, which is why this is RESIDUAL and not SHIPPED: (1) the evidence is a PROXY -- absence of a nearby cold event -- not the direct positive, which is the absorbing request's own cacheRead>0; (2) the denominator is 2 sessions on one machine, firings dated 2026-08-06 and 2026-08-10, so the RATE is unmeasured exactly as row 4's is; (3) the +/-180s window is inherited from this matrix's own prior join and is not itself validated -- counting any type:cold record instead of cc+cause-bearing ones moves the near-count from 31 to 54 without changing the verdict, since the extra matches are k:gauge context samples, not re-bills."
  },
  "24": {
    "status": "DECLINED",
    "evidence": "docs/directives/robustness-threat-matrix.md",
    "date": "2026-08-05",
    "residual": "The messages-layer mitigation is REFUSED ON MEASUREMENT (2026-08-05, corpus-wide), and the row's own bound is that mid-session corpus edits are FREE — only the resume pays. The system-prompt half is NOT declined and NOT to be built yet: pinning a stale SYSTEM prompt is a far stronger claim than a stale tool description, and the cell forbids building either remaining half before the volatile-identifier root cause is settled. Durable evidence is a SYNTHETIC fixture, booked — the walk's findings rest on a literal text predicate the scrub destroys, so a pin would replay green and prove nothing."
  },
  "25": {
    "status": "RESIDUAL",
    "evidence": "65d0455",
    "date": "2026-08-05",
    "residual": "The fix is proven live, and proven by DIFFERENCE rather than by a green: the same capture replays with exactly two stability violations under the pre-fix build (76658d8) and clean under the shipped one. Residual, PARKED: eviction stranding — the relocation memory is LRU/prune-capped at 256 conversations, so a conversation quiet past both caps loses its block with the prefix INTACT, which is the original full-cost flip. Promotion evidence is named: a costly departure row whose predecessor declared relocated."
  },
  "26": {
    "status": "RESIDUAL",
    "evidence": "246b61d",
    "date": "2026-08-11",
    "residual": "Shipped and SERVING (verified 2026-08-11 content-to-content: proxy source fingerprint 140351b73356 == /health proxy_tree; pin ebaaf0e == HEAD:proxy). Two remainders. (1) THE BRIDGE: the old-key dual-read retires after seven consecutive days with zero oldKeyFallback records; measured 2026-08-11, zero across the snapshots dir over 110 event files written since the restart, so the instrument is live rather than dead; earliest discharge 2026-08-17. (2) ABSORPTION IS UNASSERTED: no bite covers the entry's own criterion that a relocating request serves rewrite/unchanged instead of no-baseline with forwarded tools[] byte-identical, and identityRotations.transitions does NOT answer it — that counter compares raw against forwarded messages[0], i.e. our pipeline still relocating, which this fix deliberately does not stop. Home for both: the booked gate-live snapshots pass."
  },
  "27": {
    "status": "ACCEPTED",
    "triage": "CONTROLLED-CAUSE",
    "evidence": "docs/directives/robustness-threat-matrix.md",
    "date": "2026-08-07",
    "residual": "Controlled cause: the entry is gone by expiry before the request is composed, and the operator initiated it by being away — the one place FORK-NOTES' 'any non-operator-initiated bust is a prevention target' does not reach. What IS a target is the MISCLASSIFICATION: such an event booked as k:hit and triaged KNOWN-OPEN row 4, inflating row 4's evidence with an instance it did not cause. The discriminator is computable from the ledger record bust-triage already reads: gap > TTL together with a surviving read (ctx - cc) of ~0 — not mtok."
  },
  "28": {
    "status": "SHIPPED",
    "evidence": "403dde9",
    "date": "2026-08-07",
    "residual": null
  },
  "29": {
    "status": "UNASSESSED",
    "evidence": "docs/directives/robustness-threat-matrix.md",
    "date": "2026-08-08",
    "residual": "CC-side by construction — the divergence is present in the raw pre-pipeline capture, and nothing on our side can restore a prefix CC rebuilt. Mitigability stays unassessed under a standing gate: no mitigation may be designed for this row until the attribution line is emitted mechanically. Two reachable halves are named instead — PRICING the edit beforehand (restart-exposure is the right tool and cannot express this class today: it takes a TEXT predicate, booked) and BATCHING corpus and commit activity rather than spreading it across live idle sessions, since our own commits and dirty files regenerate the <env> block that invalidates every co-tenant session's cached system prefix."
  },
  "30": {
    "status": "RESIDUAL",
    "evidence": "03398e3",
    "date": "2026-08-11",
    "residual": "SHIPPED AND SERVING 2026-08-11 (fork 03398e3; dotfiles pin ec05377; verified content-to-content — /health proxy_tree 53180b0da2ee equals proxy/source-fingerprint.mjs over the deployed tree, and the pin equals HEAD:proxy). VERIFIED LIVE by the post-deploy sweep (started 13:49:40Z, finished 14:19:08Z, code stamp proxyTree 53180b0da2ee so VERIFIED equals RUNNING): conservation is ZERO across ALL 25 captures, against 12 failing captures and 1,899 rows before the fix. That supersedes the pre-deploy five-capture claim rather than repeating it — the sweep covers every capture still on disk, which is a larger population than the five replayed by hand. The sweep's one remaining failing row is s-captureBC's single stability violation, already closed as NON-DEFECT the same day (model switch across the pair, cacheRead 0, its declared modelChangedAcrossPair exemption booked and unbuilt) — a known row awaiting its exemption, not a consequence of this fix. RESIDUAL, and now for ONE reason only: the carried-over set is stable only while fresh-session-sort's relocation memory holds it, so row 25's eviction-stranding residual reaches this row — an evicted memory stops declaring the block and CC's own bytes win, which is correct but untested here. Promotion to SHIPPED wants a bite over that eviction path."
  }
}
